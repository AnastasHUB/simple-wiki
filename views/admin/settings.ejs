<% title='Paramètres'; %>
<h1>Paramètres</h1>
<form method="post" class="card">
  <%- include('../partials/csrf_field') %>
  <label>Nom du wiki</label>
  <input type="text" name="wiki_name" value="<%= s.wiki_name %>" />
  <label>Logo URL</label>
  <input type="url" name="logo_url" value="<%= s.logo_url %>" />
  <label>Discord Webhook Admin</label>
  <input type="url" name="admin_webhook_url" value="<%= s.admin_webhook_url %>" />
  <label>Discord Webhook Feed</label>
  <input type="url" name="feed_webhook_url" value="<%= s.feed_webhook_url %>" />
  <label>Pied de page</label>
  <input type="text" name="footer_text" value="<%= s.footer_text %>" />
  <fieldset class="form-section">
    <legend>Monétisation</legend>
    <p class="field-hint">
      Configurez Google AdSense pour afficher des publicités. Laissez les champs vides pour désactiver les annonces.
    </p>
    <label>Identifiant éditeur Google AdSense</label>
    <input
      type="text"
      name="adsense_publisher_id"
      placeholder="ca-pub-XXXXXXXXXXXXXX"
      value="<%= s.adsense_publisher_id %>"
      autocomplete="off"
    />
    <label>Emplacement bannière (ex.&nbsp;: haut de page)</label>
    <input
      type="text"
      name="adsense_top_banner_slot"
      placeholder="1234567890"
      value="<%= s.adsense_top_banner_slot %>"
      autocomplete="off"
    />
    <label>Emplacement intégré au contenu</label>
    <input
      type="text"
      name="adsense_incontent_slot"
      placeholder="1234567890"
      value="<%= s.adsense_incontent_slot %>"
      autocomplete="off"
    />
    <label>Extrait de validation AdSense</label>
    <textarea
      name="adsense_verification_code"
      rows="4"
      placeholder="&lt;script async src=...&gt;&lt;/script&gt;"
      autocomplete="off"
    ><%= s.adsense_verification_code %></textarea>
    <small class="field-hint"
      >Collez l'extrait de code de validation fourni par Google pour prouver la propriété du
      domaine. Il sera injecté dans l'en-tête &lt;head&gt; de toutes les pages.</small
    >
  </fieldset>
  <label>Dépôt GitHub pour le changelog</label>
  <input
    type="text"
    name="github_repo"
    placeholder="owner/repo ou URL https://github.com/…"
    value="<%= s.github_repo %>"
  />
  <small class="field-hint"
    >Laissez vide pour désactiver l'onglet Changelog. Le dépôt doit être public.</small
  >
  <label>Source du changelog</label>
  <select name="github_changelog_mode">
    <option value="commits" <%= s.github_changelog_mode === 'commits' ? 'selected' : '' %>>
      Commits
    </option>
    <option value="pulls" <%= s.github_changelog_mode === 'pulls' ? 'selected' : '' %>>
      Pull requests
    </option>
  </select>
  <div class="actions"><button class="btn success" data-icon="💾">Enregistrer</button></div>
</form>
